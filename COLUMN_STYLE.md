# コラム記事の書き方（このリポジトリの決まり）

新しいコラムを追加するときは、既存の `column-*.html` をひな形にして、
この文書のルールに従ってください。

## ファイル

- ファイル名：`column-〇〇.html`（〇〇は内容がわかる英小文字）
- ヘッダー・フッター・パンくずは既存記事からそのままコピーする
- `<head>` に `title` / `meta description` / `canonical` / OGP / Article構造化データを入れる

## 記事の構成（この順番）

1. **ページ見出し**（`.page-hero`）… パンくず、日付＋カテゴリ、`<h1>`
2. **冒頭のごあいさつ**（`.col-intro`）… ★下記のとおり必ず入れる
3. **目次**（`.toc`）
4. **本文**（`.article-body`）… 4〜5章。各章は `<h2 class="ch-head">`
5. **CTA**（`.article-cta`）＋ 一覧へ戻るリンク（`.article-back`）

## ★ 冒頭のごあいさつ（必須）

読者に語りかける導入にする。次の4つを必ずこの順で書く。

※「こんにちは。水戸の若手提案型税理士事務所の日下部税理士事務所です。」といった
　あいさつ・名乗りは**入れない**。いきなり本題から始める。

1. `今回は〜について解説をしていきます。` ＋ そのテーマの背景を2〜3文
2. `このコラムを読むと…`
3. **読者が抱く疑問を2〜3個**、`.col-qbox` の箇条書きで。
   話し言葉の疑問文にする（例：「うちの場合、いくらまで税金がかからないんだろう？」）。
   専門用語を使った問いにしない。
4. `このようなことが理解できるようになります。とても分かりやすく簡潔にまとめましたので、ぜひ最後まで読んでくださいね。`
   ※**「およそ◯分程で読める」といった読了時間は書かない**（記事の長さを先に示すと読者が身構えるため）

```html
<div class="col-intro" data-reveal>
  <p>今回は〜について解説をしていきます。<br>
  （テーマの背景を2〜3文）</p>
  <p class="ci-lead-in">このコラムを読むと…</p>
  <div class="col-qbox">
    <ul>
      <li>〜なんだろう？</li>
      <li>〜だろうか？</li>
    </ul>
  </div>
  <p>このようなことが理解できるようになります。とても分かりやすく簡潔にまとめましたので、ぜひ最後まで読んでくださいね。</p>
</div>
```

※「お知らせ」カテゴリの記事（`column-renewal.html` など）は対象外。
　従来どおり `.article-lead` を使う。

## 図・表（★必須）

**すべての記事に、図・表を合わせて3点以上入れること。** 文章だけの記事にしない。
読者が理解につまずきやすい箇所の直前・直後に置く。

入れるべき場面の例：
- 制度Aと制度Bの違い → **比較表**
- 「結局いくら得なのか」 → **具体例＋計算過程の表＋棒グラフ**
- 期間・期限の話 → **時系列の図**
- 家族関係が出てくる → **親族図**
- 選び方・使い分け → **「こんな場合はこちら」の表**

### 制度を解説するときの鉄則

**「結局いくら変わるのか」を必ず数字で示す。** 制度の説明だけで終わらせない。
前提（財産額・相続人）を置いた具体例を作り、対策前後の税額を表とグラフで比較する。

**効果が小さい・効果がない場合も、正直にそう書く。**
読者にとっては「やっても意味がない」ことを知るのが最も価値がある。
（例：相続時精算課税の2,500万円の特別控除は、相続時に加算されるため相続税は減らない）

- **図**：`<figure class="col-fig col-fig--framed">` の中にインラインSVG ＋ `<figcaption>`
  - **viewBoxの横幅は400前後**にする（PCとスマホの両方で文字が読めるサイズになる）
  - **SVG内の文字は font-size 14以上**。これ未満はスマホで読めない
  - 棒グラフの数値ラベルは棒の中に白文字で置くと重ならない
- **表**：`<table class="col-table">`。数値セルは `class="num"`、合計行は `<tr class="is-total">`

## 強調表示（赤文字・黄色マーカー）

読者に必ず持ち帰ってほしい一文だけを強調する。**使いすぎると強調の意味がなくなる。**

| 使うクラス | 見た目 | 使いどころ |
|---|---|---|
| `<strong class="em-red">` | 赤の太字 | 誤解していると損をすること、警告 |
| `<strong class="em-mark">` | 黄色マーカー（下線風） | 覚えて帰ってほしい結論・定義 |
| `<strong>` | 黒の太字 | それ以外の通常の強調 |

ルール：

- **1章につき1か所まで。** 記事全体で10か所程度が上限
- **赤とマーカーを同じ文に重ねない**（読みにくくなる）
- 赤は `#c62828` を使う。**純粋な赤 `#ff0000` は使わない**
  （白背景でコントラスト比4.0:1しかなくWCAG AA基準の4.5:1を満たさない。`#c62828` は5.6:1）
- 強調するのは**文**であって単語ではない。単語だけ赤くしても意味が伝わらない
- 数字そのもの（「1,278万円」など）は表と図で見せる。本文で赤くしない

## 数字を書くときの鉄則

- 税額・料金・控除額は**必ず検算**してから書く
- 前提を単純化した計算例には、その旨と「実際は個別事情で異なる」注記を入れる
- 記事末尾に `<p class="article-note">` で
  「※本記事は一般的な解説であり、〇年〇月時点の情報に基づいています。…」を必ず入れる

## 公開時に一緒に更新するファイル

- `column.html` … 一覧の先頭に `.news-item` を追加（`data-cat` にカテゴリ名）
- `index.html` … トップの新着一覧の先頭にも追加（一覧は5件に保ち、いちばん古いものを外す）
- `sitemap.xml` … 新記事のURLを追加

## 公開日を先に延ばす場合（予約投稿）

このサイトに予約投稿の機能はないため、次の4か所で「隠す／出す」を切り替える。

隠すとき（記事ファイルは残したまま）：

1. `column-〇〇.html` の `<head>` に `<meta name="robots" content="noindex, nofollow">` を入れる
2. `column.html` の該当 `.news-item` を HTMLコメント `<!-- -->` で囲む
3. `index.html` の該当 `.news-item` を HTMLコメントで囲む（一覧が5件になるよう調整）
4. `sitemap.xml` の該当 `<url>` をHTMLコメントで囲む

公開日に出すとき：上の4か所のコメント記号と `robots` の行を外して push するだけ。
